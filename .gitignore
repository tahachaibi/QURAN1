node_modules/
.expo/
dist/
web-build/
*.log
npm-debug.*
yarn-error.*
.DS_Store
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
.env
.env.local
# Anchored to the repo root ON PURPOSE. An unanchored "android/" matches a
# directory of that name at ANY depth, which silently excluded the whole native
# module at modules/expo-arabic-speech/android/ — the Kotlin was on disk, passed
# every local check, and was never committed.
/android/
/ios/
coverage/

# raw hadith dataset, fetched by docs/hadith.md; only the generated assets are committed
/hadith-source/

# Signing keys. NEVER in the repository — a committed upload key, even in a
# deleted commit, is in the history forever and can sign anything claiming to be
# this app. store/README.md says where it should live instead.
*.jks
*.keystore
keystore.properties
